import React from 'react';

const FacebookButton = props => (
  <div className='FacebookOAuthButton' style={{textAlign: 'center'}}>
    <button>Login With Facebook</button>
  </div>
)

export default FacebookButton;